import React from 'react';
import './LoginStats.css'

const LoginStats = () => {
    
    return (
        <div>
            <h6 className="card-title">로그인 통계</h6>
        </div>
    );
};

export default LoginStats;
